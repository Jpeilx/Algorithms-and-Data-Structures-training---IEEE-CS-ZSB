#include <iostream>
#include <string>
#include <vector>
using namespace std;
 
int main() {
 
    cin.tie(0);
    ios::sync_with_stdio(0);
    int c ;
    cin>>c ;
    vector<string>s(c) ;
    for (int i = 0 ; i<c ; i++)
        getline(cin,s[i]) ;
    for (int i = 0 ; i<c ; i++)
        cout<<"gzuz\n";
}
